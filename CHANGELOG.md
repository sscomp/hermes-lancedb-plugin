# Changelog

## 0.1.1

- Rename the distribution repo to `lancedb-pro-hermes-plugin`.
- Keep the Hermes provider name as `hermes_lancedb`.
- Clarify that this repo is the formal install target for new Hermes machines.
- Preserve source-side `timestamp` and metadata when migration-oriented imports
  provide them through the bridge.

## 0.1.0

- Initial Hermes-native LanceDB memory provider scaffold.
